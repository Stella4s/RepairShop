@model IEnumerable<RepairShop.Models.CatlPart>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CatlPartId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CatlPartId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CatlPartId })
        </td>
    </tr>
}

</table>
